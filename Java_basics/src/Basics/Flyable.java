package Basics;

public interface Flyable {
    public void fly();

    // interface methods are called abstract methods
    // an interface is a contract and must be executed by the class implementing it
}
